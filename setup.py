from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Extracting name of the entities (environmental consultants) from the cover page of PDF Documents',
    author='Nipun Goyal',
    license='MIT',
)
